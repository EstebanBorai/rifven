## v0.1.0

- Add `Rif` `struct` with validation
- Add `FromStr` implementation for `Rif`
- Add `Kind` `enum` with support for foreigner, government,
legal entity, passport, township and venezuelan types
- Add `FromStr` implementation for `Kind`
- Add capability to calculate the _check number_